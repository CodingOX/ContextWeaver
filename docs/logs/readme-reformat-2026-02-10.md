# README 排版与编号整理记录（2026-02-10）

## 背景

针对用户反馈：当前 `README.md` 的标题层级、编号连续性与留白不统一，阅读路径不够清晰。
本次仅进行文档结构化排版，不调整内容顺序与语义。

## 调整范围

- 文件：`README.md`
- 类型：Markdown 排版优化（标题层级、编号、空行）
- 约束：不改段落顺序，不改命令与说明含义

## 变更摘要

### 1) 标题层级统一

- 将 `MCP 集成配置` 并入“二、使用”章节，调整为子标题：`#### 2) MCP 集成配置`
- 将其子项细化为：
  - `##### 2.1 Claude Desktop 配置`
  - `##### 2.2 Codex CLI 配置`
- 将 `MCP 工具说明` 统一为编号章节：`### 五、MCP 工具说明`

### 2) 编号连续性修正

- “二、使用”章节内步骤统一为 `1 ~ 5`
- “三、测试”章节内统一为：
  - `#### 1) 安装后 CLI 验证`
  - `#### 2) 安装常见问题（FAQ）`
  - `#### 3) 项目回归测试（开发者）`
- FAQ 统一为 `Q1 ~ Q4`（移除 `Q0`）

### 3) Markdown 留白整理

- 清理多处重复空行，增强终端阅读紧凑性
- 保持代码块与段落之间的标准间距

## 影响评估

- 对外功能：无影响
- 命令行为：无影响
- 文档可读性：提升（章节结构更清晰，编号可追踪）

## 验证

- 已检查 `README.md` 的标题层级与编号连续性
- 已确认本次未调整正文内容顺序
